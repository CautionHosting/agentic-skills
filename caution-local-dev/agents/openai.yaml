interface:
  display_name: "Caution Local Dev"
  short_description: "Run, build, and debug the Caution platform services locally"
  default_prompt: "Use $caution-local-dev to bring up the Caution platform (api/gateway/frontend + Postgres) locally on a Linux amd64 host/VM, fix a Docker cgroup-driver issue, or generate an alpha code to register."
